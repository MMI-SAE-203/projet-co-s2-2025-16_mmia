---
const {...post} = Astro.props;
---

    <a href={`/forum/${post.id}`}><div class="flex flex-wrap *:mb-24  *:text-[var(--blanc-casse)] dark:*:text-[var(--gris-anthracite)]">
        <div class="w-[393px] h-[342px] bg-[var(--gris-anthracite)] dark:bg-[var(--blanc-casse)] text-center rounded-2xl mx-12">
            <p class="text-3xl font-bold my-5 mx-3">{post.Titre}</p>
            <p class="mt-16 text-3xl font-bold font-poppins">{post.Specialite}</p>
            <div class="flex justify-evenly mt-4">
                <p class="text-2xl">{post.Nb_Fav} réponses</p>
                <p>keur</p>
            </div>
        </div>
    </div>
    </a>