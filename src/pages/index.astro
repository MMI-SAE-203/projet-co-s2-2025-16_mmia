---
import Layout from "../layouts/Layout.astro";
import MMIA from "../assets/icones/mmi-white.svg";
import Fleche from "../assets/icones/fleche.svg";
import Paperclip from "../assets/icones/paperclip.svg";
---


<Layout>
  <script src="../../script/app.js"></script>
  <main>
    <div class="hidden lg:flex banner h-screen flex-col justify-center items-center gap-5 text-center">
            <canvas id="dotsCanvas" class="absolute w-full h-full top-0 bottom-0 pointer-events-none"></canvas>
            
      
      <div class=" flex flex-col">
        <h1 class="flex p-4 font-bold text-7xl">
          Du mal en MMI  ?
        </h1>
       <p class="text-7xl font-bold"><MMIA class="inline-block" /> est là</p>
        <div class="flex bg-white justify-center rounded-2xl p-3 mt-20">
          <a class="text-[var(--gris-anthracite)] flex  text-3xl font-bold   p-2 px-3 " href="/Chat"
            >Commencez maintenant ! <Fleche class="ml-40" />
          </a>
        </div>
      </div>
      <span></span>
      </div>

      <div
        class="bg-[var(--blanc-casse)] flex justify-center  h-20 w-250 rounded-2xl items-center m-30 "
      >
        <p class="text-[var(--gris-anthracite)] flex items-center  text-3xl ">
          <Paperclip class="mr-20" />Qu’apportons nous de plus que certaines IA génératives ?
        </p>
        <span class="text-[var(--gris-anthracite)] animate-pulse ">|</span>
      </div>

      

      <div class="">
        <div class="flex flex-col pb-30">
          <p class="text-5xl pl-6 m-8 font-bold">Une révolution de l'éducation !</p>
          <div class="items-center lg:flex flex-row  m-4">
            <img
              class=" rounded-3xl bg-[var(--blanc-casse)] lg:h-160 w-200 border-10"
              src="/src/assets/images/accueil1.webp"
              alt=""
            />
            <p class="text-center">
              Grâce à notre IA spécialisée dans les cours de MMI, entrez dans un
              nouveau monde !
            </p>
          </div>
          <div class="flex justify-center">
            <button
              class="text-white bg-[var(--violet)] m-5 rounded-2xl border-20 border-[var(--violet)] text-xl font-bold"
              >À vos prompts !</button
            >
          </div>
        </div>

        <div class="flex-row-reverse  pb-30">
          
             <p class="text-right pr-22 text-5xl font-bold m-8">Une communauté solidaire !</p>
             <div class="  lg:flex flex-row-reverse items-center m-4">
               <img
                 class="h-160 w-200 border-10 rounded-3xl bg-[var(--blanc-casse)]"
                 src="/src/assets/images/accueil2.webp"
                 alt=""
               />
               <p class="">
                 Découvrez toute une communauté solidaire constituée d’étudiants en
                 MMI de tout niveau !
               </p>
             </div>
             <div class="flex justify-center">
               <button
                 class="text-white bg-[var(--violet)] m-5 rounded-2xl border-20 border-[var(--violet)] text-xl font-bold"
                 >Au forum !</button
               >
          
            </div>
          </div>
          <div class="flex flex-col pb-30">
            <p class="text-5xl font-bold  m-8">Une manière de travailler ludique !</p>
            <div class="items-center lg:flex flex-row  m-4">
              <img
                class="h-160 w-200 border-10 rounded-3xl bg-[var(--blanc-casse)]"
                src="/src/assets/images/accueil3.webp"
                alt=""
              />
              <p class="text-center">
                Apprenez à travailler au mieux en MMI grâce à MMIA
              </p>
            </div>
            <div class="flex justify-center">
              <a href=""
                class="text-white bg-[var(--violet)] m-5 rounded-2xl border-20 border-[var(--violet)] text-xl font-bold"
                >J'y vais !</a>

            </div>
        </div>
      </div>

      <div class="bg-[var(--blanc-casse)]  ">
        <p class="text-center text-[var(--gris-anthracite)] font-bold text-4xl m-5">Nos tarifs</p>
        <div class="flex gap-30 justify-center items-center mx-6 ">
        <div class="bg-[var(--gris-anthracite)] p-14 rounded-2xl h-150 w-100 flex flex-col my-10">
  <div class="pb-4">
    <p class="text-white pb-2 font-bold text-center">Basique</p>
    <p class="text-[var(--gris-anthracite)] text-center bg-[var(--blanc-casse)] rounded-4xl pt-2 pb-2">0 € / mois</p>
  </div>
  <ul class="pt-3 mb-6 justify-center pl-3 flex-grow *:text-center">
    <li class="text-white my-10">Accès à l'IA</li>
    <li class="text-white my-10">Forum en ligne</li>
    <li class="text-white my-10">Prompts limités</li>
  </ul>
  <a href="" class="bg-[var(--gris)] border-[var(--gris)] border-10 rounded-2xl text-center font-semibold pl-2 pr-2 block">En profiter</a> 
</div>
        <div class="bg-[var(--gris-anthracite)] p-14 rounded-2xl h-150 w-100 flex flex-col">
  <div class="pb-4">
    <p class="text-[var(--violet)] pb-2 font-bold text-center">Premium</p>
    <p class="text-[var(--gris-anthracite)] text-center bg-[var(--blanc-casse)] rounded-4xl pt-2 pb-2">10 € / mois</p>
  </div>
  <ul class="pt-3 mb-6 justify-center pl-3 flex-grow">
    <li class="text-white mb-10">Plus de prompts disponibles</li>
    <li class="text-white mb-10">Personnalisation plus complète</li>
    <li class="text-white mb-10">Et plus encore</li>
  </ul>
  <a href="" class="bg-[var(--violet)] border-[var(--violet)] border-10 rounded-2xl text-center font-semibold pl-2 pr-2 block">En profiter</a> 
</div>
        <div class="bg-[var(--gris-anthracite)] p-14 rounded-2xl h-150 w-100 flex flex-col">
  <div class="pb-4">
    <p class="text-white pb-2 font-bold text-center">Entreprise</p>
    <p class="text-[var(--gris-anthracite)] text-center bg-[var(--blanc-casse)] rounded-4xl pt-2 pb-2">Tarif modulable
</p>
  </div>
  <ul class="pt-3 mb-6 justify-center pl-3 flex-grow">
    <li class="text-white my-10">Tous les avantages du premium</li>
    <li class="text-white my-10">tarif de groupe</li>
  </ul>
  <a href="" class="bg-[var(--gris)] border-[var(--gris)] border-10 rounded-2xl text-center font-semibold pl-2 pr-2 block">En profiter</a> 
</div>
        </div>
      </div>

      <script>
        const canvas = document.getElementById('dotsCanvas');
        const ctx = canvas.getContext('2d');
        
        function resizeCanvas() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }
        
        resizeCanvas();
        window.addEventListener('resize', resizeCanvas);
        
        const dots = [];
        const numDots = 50;
        
        for (let i = 0; i < numDots; i++) {
            dots.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                vx: (Math.random() - 0.5) * 2,
                vy: (Math.random() - 0.5) * 2,
                radius: Math.random() * 3 + 1,
                opacity: Math.random() * 0.5 + 0.2
            });
        }
        
        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            dots.forEach(dot => {
                dot.x += dot.vx;
                dot.y += dot.vy;
                
                if (dot.x < 0 || dot.x > canvas.width) dot.vx *= -1;
                if (dot.y < 0 || dot.y > canvas.height) dot.vy *= -1;
                
                ctx.beginPath();
                ctx.arc(dot.x, dot.y, dot.radius, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(255, 255, 255, ${dot.opacity})`;
                ctx.fill();
            });
            
            requestAnimationFrame(animate);
        }
        
        animate();
    </script>
    

    
  </main>
</Layout>
