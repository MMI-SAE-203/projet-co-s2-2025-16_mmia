---
import Layout from "../../layouts/Layout.astro";
import { getUser } from "../../../backend/backend.mjs";
const { id } = Astro.params;
const user = await getUser(id);
if(!user){
   return Astro.redirect('/Profil');
};


// console.log(post);
console.log(user);
---

<Layout>

</Layout>

<!-- {user.nom} {user.prenom} {user.img} {user.specialite} {user.Pseudo} {user.age} {user.email} -->